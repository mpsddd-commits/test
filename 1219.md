# 자바스크립트는 일단 헤드 안에 스타일 하듯이 기입하고 정리한다.
# 스크립트 쓰는 순서에 따라 기입이 되고 문서 형식은 문서.행위(내용) 으로 구성된다. 1.직접 html헤드 안에 쓴다. 2. 외부파일을 이용한다.
# 스트링이 제일 큰 범위이고 그 안에 숫자,bigint 가 있다 숫자만 따로 항목 분리하는 이유는 연산하기 위해서 분리했다. 특수기호도 문자열 안에 다른 영역으로 포함 된다. 
# 5+1=6 5+"1"=51(1을 문자로 바뀐걸로 인식되어 5 뒤로 나열되는것)이 되는게 주요이유
# 5=="5" ture 수치로만 보았을 때 같다. 5==="5" 자료형까지 비교해서 문자열5와 숫자형 5는 다르다. ->자바스크립트에만 가능한 것.(console)에서 확인가능
# 자바스크립트에는 4칙연산 이외에 몫과 나머지를 구할 수 있다.
# - number    : 숫자(정수, 실수)
#		- bigint 메모리 사양값이라 생각하면된다.    : 정수 -(2의 53승 -1) ~ 2의 53승-1
#		- string    : 문자열
#		- boolean   : 논리값(true, false)
#		- undefined : 초기화되지 않은 상태, 변수 초기화에 사용 -자바스크립트에만 사용 입력값에서 출력값이 없을때 document.write(입력값) 함수 입력값이 없을때
#		- null      : 객체 변수 초기화 사용
#		- NaN       : Not a Number -숫자로 정의 되지 않았을때 예를 들어 0조차 기입되지 않았을때 -파이썬에서 저런 모습이 나올 때가 있다. -> 표기값 0으로 바꾸던가 예외사항으로 바꾼다 ->전처리방법

#    [참조 타입] 참조형, Reference Type - 기본 자료형 아닌 나머지들(객체형이라 부른다)
#		- array
#		- function
#		- ...
#		*/
#   // [변수선언] 변수를 담는 그릇 -> 숫자 이외에도 모든 것을 넣을 수 있다.
#		// var 변수명; 후 변수명 = 1 or "글", null 다 가능
#  typeof 는 자료형의 유형 설명하는 명령어
# var은 변수의 크기와 범용성이 너무 커서 let/const의 두개로 분리하여 사용한다. let은 기존var과 거의 유사하지만, const는 상수이므로 값을 설정하고나면 변화하지않는다.
# 화살표 함수 (Arrow Function) 함수표시로 function 풀네임쓰기 힘드니까 => 로 표시한다.

# var age = 35; (선언 및 할당)
#   var name = '홍길동';

#  문자의 조합으로 표시하는 방법 2가지 1.  var message = '저는 ' + name + '이고, 나이는 ' + age + '세 입니다.';
#  2. var messageTwo = `저는 ${name}이고, 나이는 ${age}세 입니다.`;
## ${ }은 내부의 값을 연산할 수도 있다. 문자의 구조를 합칠대 사용한다.
   
   [문자열] ===================================================================
    * 템플릿 리터럴 : 백틱 사용
    : 이중 따옴표나 작은 따옴표 대신 백틱(`)을 사용해서 표현
    : 작은 따옴표와 혼동할 수 있으나, 백틱은 일반적으로 키보드 tab 키 위에 있음
    : 플래이스 홀더를 이용한 ${expression}으로 표현식 사용
    ============================================================================

# 문자는 무조건 싱글 아니면 ''표시가 된다.
# 인덱스는 무조건 0부터 시작하고 숫자는 1부터 시작한다.
# 자바스크립트는 인덱스 능가한 숫자가 마지막에 표현이 되더라 
# "good" -> 인덱스적 해석 === 0G1O2O3D4~5
# 음수 인덱스는 제일 끝 부분에서부터 0으로 시작한다.
# split은 문자열에서만 사용이된다.
# split('')에서 ''는 각 문자별로 하나하나 다 쪼개라는 구분자이다 -> 문자열을 뽑기 위한 구분자이다.
# 나중에 문자열을 찾기 위해서 쓰는 코드양식으로 today.substring(today.indexOf('o'), today.length)
# a.trim().replaceALL(" ","") ->모든 공백제거
# trim()의 경우 글자의 앞 뒷부분 공백이 많은 부분을 제거할 때 사용하면된다.

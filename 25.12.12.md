# http와 https의 차이 http는 일반, https는 인증 사이트에 인증을 받지만 유료서비스
# localhost일때는 80이 기본port이다. 5500과 80의 차이를 알고 이해한다.(port)이기 때문에
# url과 uri의 차이점 알기
# html은 문서 양식과 비슷하다.
# front파트는 html(문서양식),css(스타일링),javascript(동적프로그래밍) -3가지를 다룬다
# framework는 규격화 react가 대표적인데 front파트를 어느정도 숙지 해야한다. java script기반으로 만들어짐
# 디자이닝 ->퍼블리셔 -> 프론트엔드
# React -> 메타(전 페이스북)에서 만든 프로그램
# 유튜브에 리액트 관련 프로그램 좀 알아보고 배우기
# 그리고 !important는 강제성을 띄는 것이라 우선순위 배제하고 제일 최우선 진행함 (스타일에서만 사용 하는 태그)
# style padding에서 두가지로만 쓰면 탑바텀 , 좌우로 사용 되고 . 4가지로 쓰면 숫자배열은 탑 우 아래 좌 된다.
# 그리고 f12로 코드를 보면서 스타일링을 확인가능하다.
# 

# iframe -  지도 유튜브 등의 자료를 퍼올 때 쓰는 도구
# 보고서 등의 용도로 사용이 가능하고 여러가지 화면을 퍼와서 하나의 큰 사이트를 만드는 것도 가능.
# 단점은 잘 못가져 올 경우도 있음. 개발단계 때 테스트 후 ui를 가져오는 것이 필요하다.
# 선택자. 크롤링(웹페이지 수집할때) 할 때에 필요하다. EX) SPAN 등
## span 등의 경우 같은 설정에서는 마지막에 설정된 값이 나온다 (색깔 등) + 범주를 달리하여도 뒷배경 색은 같은 내부area가 있더라도 마지막에 적용된 것이 우선순위로 결정된다(덮어씌워진다 생각!). 색깔 이외의 선택자에서도 이게 적용되는가?
## = 전체 일치 ^= 시작하는		$= 끝나는		*= 일부 문자 (단어 중 일부 문자)		~= 단어 일치
# 선택자를 이용하여 스타일 변환을 할 때 보통 바디에서 선택자를 바꾸는 것 보다는 헤드 쪽에서 스타일을 변화시키는게 좋다
# content 는 글자나 그림을 넣을수 있다.
# em ~배로 키워라
# 색의 경우 f12를 통해 style의 색을 퍼온다! #번호를 쓰거나 rgb rgba등으로 사용 가능하다.
# 보통 글자 선택시 px단위보다는 em단위로 사용하면 좋다. 각 프로그램마다 정해져 있는 px가 존재하기 때문에. 그리고 rem이 있긴한데 화면 배율을 볼만큼 그게 볼 일이 없으니 자주 사용하지 않는다.
# 구글 폰트에서 무료 폰트를 사용해서 폰트 변경을 하면 된다.
# vw는 너비에 따른 크기가 바뀌는데 확대하는것과는 관련이 없다
# 앱솔루트를 쓰려면 위치를 지정해야한다. 부모를 지정하거나 아니면 바디 기준으로 릴레티브주고 자식 가져갈때 부모로 부터 시작된다
# 이와 비슷한게 fixed가 있는데 위치 지정 파트를 잘 보고 해보자
[172.27.192.1](http://172.27.192.1:5500/02%20WEB/WEB_STUDY-study/frontend/)

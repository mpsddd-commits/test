#깃허브는 소스코드를 올리고 공유기를 통해 서비스 하는것
#프로토콜은 http로 한다. 보안용은 https 
#<meta charset="UTF-8"> ->인코딩이라 한다. 문자(한글·영문·기호)를 컴퓨터가 이해하는 숫자(바이트)로 바꾸는 규칙입니다.
	<meta name="viewport" content="width=device-width, initial-scale=1.0"> -> 반응크기 width설정값표기 방법
	<link rel="icon" href="../images/logo.png"/> -> 탭 위의 로고 표기 이 표식이 없을때 개발자 모드 햇을시에 x 표기 나올때도 있다. 
  <title>Document</title>   ->  탭 위의 제목 표기
##<a></a> 는 이동하느 ㄴ링크다.
##href는 글릭 대상물이고 아래쪽에 표시가 뜨면 링크 이동 경로 보이고 자바스크립트 하게 되면 아래 이동 경로가 보이지 않는다.
#index.html은 url주소에서 나타나는 메인페이지라 생각하면된다.
##index.html은 url주소에서 나타나는 메인페이지라 생각하면된다.
###index.html은 url주소에서 나타나는 메인페이지라 생각하면된다. -정말 중요. 주소값제어 확인. index와 주소이동관련내용 이해.
#pre태그 부분 다시 공부해보기
#form
##input에서 name="txt" 밸류값 설정!!!
## method : 1.get 주소에 내용물 포함 같이 보냄(게시물 공유) -누구나 봐도 될 경우, 화면에 보이는 정보, 프론트엔드에서 무조건 사용. 주소 뒷부분에 노출된다.
##          2.post 주소에 노출되지 않는 방향.(로그인) -민감한 정보의 경우(개인정보의 경우) 무조건 백엔드와 사용한다. 프론트로만으로 사용 불가(테스트가 안되기 때문에)
##화면 전환용으로도 사용가능하다.
## 1.radio vs 2.checkbox 
###1. 1가지 선택 2. 다중선택
## textarea에서 칸이 늘어나면 자동으로 늘어난다 -> 자바스크립트 이용한다. 칸이 늘어날때 옆에 스크롤 표시 뜬다 ->기본형 그냥 사용한것
## label과 input태그를 통해 하나로 묶이고 결과물이 그냥 쓴것과 차이가 있다. ex) 아이디부분에 커서를 눌러도 아이디 기입 가능한 부분으로 바로감 label for 뒤의 아이디와 input태그 안에 아이디가 같아야 하나로 묶임
##name은 전달할때 중복이 가능하지만 id는 불가하다 그리고 작성된 순서대로 보내진다. 이 부분을 다시 확인키 위해 항상 name도 동일하게 쓸쑤 잇게 하자 -> label,name,id를 다 똑같은 문자로 쓴다!!!!!!!!!!!!!!! (name은 중복가능하지만 나중에 볼 때 헷갈린다)
# 기억해놓기 :나중에 front end와 back end병행시 response를 통해 결과값이나 문제점 확인 가능하다 
